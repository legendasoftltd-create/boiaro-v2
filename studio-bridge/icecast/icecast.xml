<icecast>
    <location>Local Dev</location>
    <admin>admin@localhost</admin>
    <hostname>localhost</hostname>

    <limits>
        <clients>50</clients>
        <sources>5</sources>
        <queue-size>524288</queue-size>
        <client-timeout>30</client-timeout>
        <header-timeout>15</header-timeout>
        <source-timeout>10</source-timeout>
        <burst-on-connect>1</burst-on-connect>
        <burst-size>65535</burst-size>
    </limits>

    <authentication>
        <source-password>devpassword</source-password>
        <relay-password>devpassword</relay-password>
        <admin-user>admin</admin-user>
        <admin-password>devadminpassword</admin-password>
    </authentication>

    <listen-socket>
        <port>8000</port>
        <bind-address>0.0.0.0</bind-address>
    </listen-socket>

    <!-- Default/stationless mount (ICECAST_MOUNT in studio-bridge/.env) —
         used only when a broadcast has no station attached, e.g. an ad-hoc
         test. Real per-station mounts live in the dynamic block below. -->
    <mount type="normal">
        <mount-name>/studio-test.mp3</mount-name>
        <public>0</public>
        <fallback-mount>/fallback.mp3</fallback-mount>
        <fallback-override>1</fallback-override>
    </mount>

    <mount type="normal">
        <mount-name>/fallback.mp3</mount-name>
        <public>0</public>
        <hidden>1</hidden>
    </mount>

    <!-- One <mount> stanza per active RadioStation, regenerated by
         studio-bridge/src/icecastConfig.ts whenever stations are added,
         edited, or (de)activated in the admin panel — do not edit by hand,
         changes here are overwritten on the next sync. -->
    <!-- DYNAMIC_MOUNTS_START (managed by icecastConfig.ts — do not edit by hand) -->
    <!-- DYNAMIC_MOUNTS_END -->

    <fileserve>1</fileserve>

    <paths>
        <basedir>/usr/share/icecast2</basedir>
        <logdir>/var/log/icecast2</logdir>
        <webroot>/usr/share/icecast2/web</webroot>
        <adminroot>/usr/share/icecast2/admin</adminroot>
        <alias source="/" destination="/status.xsl"/>
    </paths>

    <logging>
        <accesslog>access.log</accesslog>
        <errorlog>error.log</errorlog>
        <loglevel>3</loglevel>
        <logsize>10000</logsize>
    </logging>

    <security>
        <chroot>0</chroot>
    </security>
</icecast>
