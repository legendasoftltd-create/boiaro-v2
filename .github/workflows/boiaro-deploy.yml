name: Remote Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # ── Build on the runner (7 GB RAM — no OOM risk) ──────────────────────
      - name: Install frontend dependencies
        run: npm ci

      - name: Install server dependencies
        run: npm ci
        working-directory: server

      - name: Generate Prisma client
        run: npx prisma generate
        working-directory: server
        env:
          DATABASE_URL: "postgresql://ci:ci@localhost/ci"

      - name: Build server
        run: npm run build
        working-directory: server

      - name: Build frontend
        run: npm run build
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID }}
          VITE_FACEBOOK_APP_ID: ${{ secrets.VITE_FACEBOOK_APP_ID }}
          VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}
          VITE_REDX_ENV: ${{ secrets.VITE_REDX_ENV }}
          VITE_APP_VERSION: ${{ secrets.VITE_APP_VERSION }}

      # ── Upload built artifacts to server via rsync ────────────────────────
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.VPS_IP }}" >> ~/.ssh/known_hosts

      - name: Clear stale dist on server
        run: |
          ssh -i ~/.ssh/deploy_key \
            ${{ secrets.VPS_USER }}@${{ secrets.VPS_IP }} \
            "rm -rf /var/www/html/staging.boiaro.com/dist && mkdir -p /var/www/html/staging.boiaro.com/dist"

      - name: Sync frontend dist
        run: |
          rsync -az --delete \
            -e "ssh -i ~/.ssh/deploy_key" \
            dist/ \
            ${{ secrets.VPS_USER }}@${{ secrets.VPS_IP }}:/var/www/html/staging.boiaro.com/dist/

      - name: Sync server dist, prisma and package files
        run: |
          rsync -az --delete \
            -e "ssh -i ~/.ssh/deploy_key" \
            server/dist/ \
            ${{ secrets.VPS_USER }}@${{ secrets.VPS_IP }}:/var/www/html/staging.boiaro.com/server/dist/

          rsync -az \
            -e "ssh -i ~/.ssh/deploy_key" \
            server/prisma/ \
            ${{ secrets.VPS_USER }}@${{ secrets.VPS_IP }}:/var/www/html/staging.boiaro.com/server/prisma/

          rsync -az \
            -e "ssh -i ~/.ssh/deploy_key" \
            server/package.json server/package-lock.json server/prisma.config.ts \
            ${{ secrets.VPS_USER }}@${{ secrets.VPS_IP }}:/var/www/html/staging.boiaro.com/server/

          rsync -az \
            -e "ssh -i ~/.ssh/deploy_key" \
            ecosystem.config.cjs \
            ${{ secrets.VPS_USER }}@${{ secrets.VPS_IP }}:/var/www/html/staging.boiaro.com/

      # ── On server: only production deps + migrate + reload ────────────────
      - name: Install production deps and reload
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 15m
          script_stop: true
          script: |
            set -euo pipefail

            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" && nvm use default

            cd /var/www/html/staging.boiaro.com/server
            npm ci --omit=dev
            npx prisma generate
            npx prisma migrate deploy

            cd /var/www/html/staging.boiaro.com
            pm2 startOrReload ecosystem.config.cjs --env production
            pm2 save
